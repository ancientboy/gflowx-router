# 开发：Vite 将 /api 代理到该地址（与 trycloudflare 隧道配合时，new-api 须在本机该端口）
VITE_DEV_PROXY_TARGET=http://127.0.0.1:3000

# 生产：若前端与 API 不同源，填写完整 API 根（须后端 CORS 允许携带 Cookie）；留空则使用相对路径 /api
# VITE_API_BASE_URL=https://api.example.com
