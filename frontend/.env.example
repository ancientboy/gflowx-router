# 前端环境变量（复制为 `.env.local` 可选）

# 生产环境若前端与 API 不同域，填写网关根 URL（不要末尾斜杠），并配置 CORS。
# 开发环境使用 Vite 代理时留空即可。
# VITE_API_BASE_URL=https://api.example.com

# 生产构建时用于 og:url；同时若设置此项，则 og:image 使用「该地址 + /og-image.svg」。
# 未设置时 og:url 默认为本仓库 GitHub 页；og:image 使用 raw.githubusercontent.com 指向本仓库内的 `frontend/public/og-image.svg`（部署到自有域名后请填写此项）。
# VITE_SITE_URL=https://router.example.com

# 可选：Google Analytics 4 衡量 ID（G-xxxxxxxx）。未设置则不加载 gtag。
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
